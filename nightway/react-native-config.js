const { projectRoot } = require("./metro.config");

module.exports = {
    project: {
        android: {
            
        },
        ios: {
            
        },
        asserts:["./assets/fonts"],
    }
}